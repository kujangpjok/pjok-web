// Nanti hubungkan ke GAS / endpoint kamu
export async function pullAll({ nip, schoolId = null }) {
  // await fetch(...); simpan ke IndexedDB
  return true;
}
export async function pushAll({ nip, schoolId = null }) {
  // baca dari IndexedDB; kirim ke cloud
  return true;
}
